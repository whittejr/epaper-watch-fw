# Arquivo .gitignore para projeto STM32 com Make/CMake e VS Code

# Diretórios de Build
# Geralmente criados pelo Make ou CMake para armazenar os arquivos compilados.
/build/
/Build/
/debug/
/Debug/
/release/
/Release/

# Arquivos de saída da compilação e linkagem
# Estes são os arquivos gerados pelo compilador e linker. O repositório deve conter apenas o código-fonte.
*.o          # Arquivos objeto
*.elf        # Arquivo executável final (com informações de debug)
*.hex        # Formato Intel HEX para gravação
*.bin        # Binário puro para gravação
*.map        # Arquivo de mapa do linker (mostra onde as coisas estão na memória)
*.lst        # Arquivo de listagem do Assembly
*.srec       # Formato S-Record
*.su         # Stack usage
*.d          # Arquivos de dependência gerados pelo GCC

# Arquivos do STM32CubeMX
# O arquivo .ioc deve ser versionado, mas outros arquivos gerados por ele não.
*.mxproject
*_hal_conf.h # Se você prefere que o CubeMX sempre gere um novo

# Configurações do VS Code
# Para evitar enviar suas configurações locais de editor/debug.
# Cada desenvolvedor pode ter as suas.
.vscode/

# Arquivos de IDEs comuns (Eclipse, STM32CubeIDE, Keil, IAR)
# Bom ter, caso você ou outra pessoa abra o projeto com outra IDE.
.project
.cproject
.settings/
*.launch
RTE/
MDK-ARM/
*.uvprojx
*.uvoptx
*.scvd
ewarm/
*.ewd
*.ewp
*.eww

# Arquivos gerados pelo sistema operacional
# Lixo comum do macOS, Windows e Linux.
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~